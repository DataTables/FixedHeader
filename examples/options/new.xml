<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="11">

<css lib="datatables fixedheader" />
<js lib="jquery datatables fixedheader">
<![CDATA[
var table = $('#example').DataTable();

new DataTable.FixedHeader(table, {
	footer: true
});

]]>
</js>

<js-vanilla>
<![CDATA[
let table = new DataTable('#example');

new DataTable.FixedHeader(table, {
	footer: true
});

]]>
</js-vanilla>

<title lib="FixedHeader">Alternative initialisation</title>

<info><![CDATA[

As well as initialisation through the `fh-init fixedHeader` configuration option when constructing the DataTable, FixedHeader can also be added to an existing DataTable using the `new DataTable.FixedHeader()` constructor. The first argument should be the DataTable to enable FixedHeader on and the second is the options for the instance.

In this example FixedHeader is attached to a DataTable with the `fh-init fixedHeader.footer` option also set.

]]></info>

</dt-example>
